package command_gui;

public class Main {

    public static void main(String[] args) {
        cmdGUI cmd = new cmdGUI();
        cmd.setVisible(true);
        cmd.setLocationRelativeTo(null);
    }
    
}
